{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No Tauri plugin permissions. Only custom invoke commands are allowed via the IPC bridge. Shell, FS, dialog, clipboard, notification, and all other plugin APIs are blocked.",
  "windows": ["main"],
  "permissions": []
}
